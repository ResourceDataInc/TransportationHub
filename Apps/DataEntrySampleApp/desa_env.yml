# This file is used to install packages for local testing
name: desa
channels:
  - snowflake
dependencies:
  - python=3.8
  - pip
  - pip:
      - streamlit>=1.28.0
      - snowflake-snowpark-python
      - plotly
