import { Polyline } from 'react-leaflet';

export const RouteLine = (props) => {
    const {} = props;

    const pathOptions = {};

    return (
        <Polyline
            // positions={}
            // pathOptions={pathOptions}
        ></Polyline>
    )
};
